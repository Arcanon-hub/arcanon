---
milestone: v0.1.9
audited: 2026-06-28
status: tech_debt
scores:
  requirements: 52/52   # functionally satisfied (15 in traceability + 37 verified via SUMMARY + integration check)
  phases: 6/6           # all merged to main (HEAD af8dfae)
  integration: 4/4      # cross-phase wiring chains WIRED
  flows: 4/4            # no broken E2E flows
gaps: {}                # no unsatisfied requirements, no code blockers
tech_debt:
  - phase: cross-cutting (process/artifact hygiene)
    items:
      - "No VERIFICATION.md for any of the 6 phases — phases were verified via /gsd-review + cross-AI codex review (*-REVIEWS.md) and CI-on-merge, not the formal verify-phase flow. Integration checker + 945/945 worker tests substitute for the missing per-phase artifacts."
      - "No VALIDATION.md for any phase — Nyquist validate-phase step hook is active but was never run (all 6 phases Nyquist-MISSING)."
      - "REQUIREMENTS.md traceability table is stale — lists only the 15 reqs for phases 129-130. The ~37 reqs for the 4 inserted phases (PV-01..18, DM-01..03, SC-01..07, MM-01..09) are tracked in phase SUMMARY frontmatter but never folded into the central traceability table."
      - "ROADMAP.md header + Roadmap Summary table are stale — the v0.1.9 milestone block still reads 'Phases 129-130, IN PLANNING' (the Phase Details section DOES carry 131-134)."
      - "STATE.md is stale — current_phase: 131 'human-verify pending' (134 actually shipped); total_phases: 7 vs 6 actual dirs; the 134-02 accumulated-context decision note still describes the SUPERSEDED METHOD_PATH_SEP control-char delimiter — the shipped code uses the exposedByMethod Map (codex round-2 caught the delimiter was forgeable). Confirmed by integration checker: METHOD_PATH_SEP is absent from query-engine.js."
  - phase: 129-shadow-trio-removal
    items:
      - "Orphaned dead fixtures: tests/fixtures/shadow/{seed.js,seed.sh} survive (headers reference the deleted shadow-scan.bats). Inert — no active test references them. RM-06 scope did not include fixture cleanup."
  - phase: 132-detectmismatches-params
    items:
      - "The 'db' canonical bucket lacks its own dedicated mismatch-exclusion test case (the 'events' bucket has one — query-engine-mismatch-params.test.js Test 4). Same SQL WHERE code path, low risk."
  - phase: 131-protocol-vocabulary
    items:
      - "Human visual verification of the live graph render (#42, plan 131-02 Task 4) never performed — code merged, automated tests green, but the db/other buckets + colors were never eyeballed in a running /arcanon:view."
---

# v0.1.9 Milestone Audit — Shadow Trio Removal & Rescan Repair (+ 4 inserted scan/mismatch fixes)

**Audited:** 2026-06-28
**Status:** `tech_debt` — all requirements functionally met, no code blockers, accumulated planning/artifact debt to clear.
**HEAD:** `af8dfae` (all 6 phases merged to main)

## Verdict

The milestone is **substantively complete and verified by other means**. Every targeted GitHub issue is closed (#42/#43/#45/#46 for the inserted phases; the shadow removal + rescan repair for the original 129-130 scope), all 6 phases are merged, the full worker suite is 945/945 green, and the cross-phase integration checker confirmed all four wiring chains are WIRED with no broken E2E flows.

What's missing is **process artifacts, not working software**: the formal verify-phase / validate-phase outputs were never generated (the team substituted cross-AI codex review captured in `*-REVIEWS.md`), and the central planning docs (REQUIREMENTS.md traceability, ROADMAP.md header, STATE.md) drifted because phases 131-134 were inserted mid-milestone via `--insert` and their requirement families were tracked in phase files rather than the central table.

This is a `tech_debt` outcome, not `gaps_found`: no requirement is unsatisfied and no flow is broken.

## Phase Scoreboard

| Phase | Title | Issue | Plans | SUMMARY status | Merged | VERIFICATION.md | VALIDATION.md |
|-------|-------|-------|-------|----------------|--------|-----------------|---------------|
| 129 | Shadow trio removal, rescan repair, docs | — | 3/3 | complete | ✅ | ❌ missing | ❌ missing |
| 130 | Regression tests & release gate | — | 2/2 | complete | ✅ | ❌ missing | ❌ missing |
| 131 | Protocol vocabulary canonicalization | #42 | 3/3 | complete* | ✅ (#44) | ❌ missing | ❌ missing |
| 132 | detectMismatches parameterized-route fix | #43 | 1/1 | complete | ✅ (#47) | ❌ missing | ❌ missing |
| 133 | Scanner backing-service client detection | #45 | 2/2 | complete | ✅ (#48) | ❌ missing | ❌ missing |
| 134 | detectMismatches method-aware matching | #46 | 2/2 | complete | ✅ (#48) | ❌ missing | ❌ missing |

\* Phase 131 SUMMARY notes "complete pending human-verify of the live graph" — automated work done, visual smoke-test outstanding.

## Requirements Coverage (3-source cross-reference)

**Total: 52 requirements, all satisfied.**

| Family | Phase | Count | In REQUIREMENTS.md traceability | In SUMMARY frontmatter | Integration-verified |
|--------|-------|-------|----------------------------------|------------------------|----------------------|
| RM-01..06, FIX-01, DOC-01..03 | 129 | 10 | ✅ | ✅ | ✅ WIRED |
| TST-01..03, VER-01..02 | 130 | 5 | ✅ | ✅ | ✅ WIRED |
| PV-01..18 | 131 | 18 | ❌ **absent** | ✅ | ✅ WIRED |
| DM-01..03 | 132 | 3 | ❌ **absent** | ✅ | ✅ WIRED |
| SC-01..07 | 133 | 7 | ❌ **absent** | ✅ | ✅ WIRED |
| MM-01..09 | 134 | 9 | ❌ **absent** | ✅ | ✅ WIRED |

The 37 requirements for inserted phases 131-134 are **complete and integration-verified** but were never added to the central traceability table — `/gsd-complete-milestone` archival should fold them in (or accept them as captured-in-phase-files).

## Cross-Phase Integration (from gsd-integration-checker)

| Wiring chain | Status | Evidence |
|--------------|--------|----------|
| Protocol vocab 131→132→134 | **WIRED** | `query-engine.js` imports `canonicalProtocol` from `ui/modules/protocol.js`; `WHERE c.protocol IN ('rest','grpc')` (q-e.js:1944,1967) consistent with `CANONICAL_PROTOCOLS` (protocol.js:46-83); http/graphql→rest aliases fold correctly |
| detectMismatches 132+134 coexistence | **WIRED** | two-sided `canonicalizePath` (q-e.js:2016,2051,2054) AND `exposedByMethod` Map (2012-2027) AND null-method path-only fallback (2043) all present; `METHOD_PATH_SEP` dead const **absent** (STATE.md note is stale) |
| Scanner→DB→mismatch 133→132/134 | **WIRED** | db/events buckets excluded from rest/grpc allowlist → chromadb/redis/kafka clients never false-flagged as endpoint_not_exposed; `manager.js:428` backing_service_deps EXCEPTION block present |
| Shadow removal completeness 129 | **WIRED** | zero residual shadow-scan/promote-shadow/--shadow/getShadowQueryEngine refs; `diffScanVersions` preserved (scan-version-diff.js:308); one inert orphaned fixture dir |

**Test suite:** worker 945/945 pass (4990ms). All referenced test files present.

## Recommended cleanup (all low-effort, none blocking)

1. **Run `/gsd-complete-milestone v0.1.9`** — archives REQUIREMENTS/ROADMAP → `.planning/milestones/v0.1.9-*.md`, records the milestone in MILESTONES.md, optionally tags `v0.1.9`. This resolves the STATE.md/ROADMAP.md drift as a side effect.
2. **Fold 131-134 reqs into traceability** (PV/DM/SC/MM) before or during archival — or explicitly accept "tracked in phase SUMMARYs" as the record.
3. **Delete `tests/fixtures/shadow/`** — orphaned dead fixtures from the removed shadow-scan.bats.
4. **(Optional) Phase 131 visual smoke-test** — eyeball db/other buckets + edge colors in a live `/arcanon:view` (#42). Code + automated tests already green.
5. **(Optional) Add a 'db'-bucket mismatch-exclusion test** mirroring the existing 'events' Test 4 in query-engine-mismatch-params.test.js.
