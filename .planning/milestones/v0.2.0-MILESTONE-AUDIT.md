---
milestone: v0.2.0
name: Scan Persistence, Pipeline & Security Hardening
audited: 2026-07-04
status: passed
scores:
  requirements: 38/38   # all v1 requirements satisfied (SEC 8, ISO 11, PIPE 4, CTR 5, INTG 4, PERF 6)
  phases: 9/9           # 135-143 all merged to main (HEAD 8a5496c); every phase VERIFICATION.md = passed
  integration: 5/5      # cross-phase wiring chains all WIRED
  flows: 4/4            # E2E flows all OK
gaps: {}                # no unsatisfied requirements, no blockers
tech_debt:
  - phase: 143 (PERF-05 / pool lifecycle)
    tracked_as: "GitHub #71 (bug, priority: medium)"
    items:
      - "Mid-scan eviction risk: the pool's sweepIdleEntries (5-min timer, 10-min TTL) stamps lastAccessed only on getQueryEngine() calls; a handle acquired at scan start is not re-touched during the scan. A scan running >10 min would have its live handle closed → the next _db.transaction() throws. Acknowledged in pool.js ('extremely unlikely at developer-tool scale') and caught by the manager writeTx error path, but that repo would SILENTLY fail rather than error loudly. Mitigation (follow-up): _touch(dbPath) at Phase B write time, or integrate the scan-lock with the sweep. FILED: GitHub #71."
  - phase: 141/140 (CTR-04 / manager-path validation)
    tracked_as: "GitHub #72 (enhancement, priority: medium)"
    items:
      - "The manager (agent) scan path does NOT pass opts.validateFindings to persistScanResult — agent output flows through parseAgentOutput (findings.js shim) rather than the canonical validateFindings gate. This is Phase 140 DESIGN INTENT (the HTTP validator guards external clients; agent output is pre-parsed), NOT a regression — but a malformed agent response could reach persistFindings without contract rejection. Consider routing agent findings through validateFindings too for defense-in-depth. FILED: GitHub #72."
  - phase: pre-existing (not v0.2.0)
    items:
      - "TODO(post-v0.1.3) in query-engine.js (~:1759): persist prose_evidence_warnings counter (needs a scan_versions.prose_evidence_warnings column). Warning-level, references formal future work; predates this milestone."
      - "Migration numbering has a benign gap at 012 (never shipped; 011→013 jump). Pre-existing; runMigrations sorts by version and skips ≤current, so no runtime impact."
  - phase: process (not code)
    tracked_as: "GitHub #73 (priority: high)"
    items:
      - "CI status checks (Worker tests / Bats / Lint / Shellcheck) are NOT *required* status checks on main — branch protection only enforces 'must be a PR'. This let PRs #64 and #68 merge with red CI (a logger ENOENT-on-fresh-install bug, fixed in PR #69). Repo-admin action: mark the four checks required in branch protection so auto-merge can't land red. Until then, CI-green must be verified manually before merge (done for #69 and #70). FILED: GitHub #73."
---

# v0.2.0 Milestone Audit — Scan Persistence, Pipeline & Security Hardening

**Audited:** 2026-07-04
**Status:** `passed` — all 38 requirements satisfied, all 9 phases verified, cross-phase integration WIRED, no blockers. Two non-blocking code warnings + process/tech-debt notes tracked below.
**HEAD:** `8a5496c` (all 9 phases 135–143 merged to `main`)

## Verdict

v0.2.0 is **complete and sound**. This is a materially stronger close than v0.1.9: every one of the 9 phases carries a `VERIFICATION.md` with `status: passed` (v0.1.9 had none), all 38 requirements are checked complete, and the cross-phase integration checker confirmed the scan write path composes correctly across six phases without any later phase silently undoing an earlier one. Full worker suite **1124/1124** green on `main`, verified both normally and with a fresh `HOME` (CI parity).

## Phase Scoreboard

| Phase | Title | Issue | Plans | VERIFICATION | Merged |
|-------|-------|-------|-------|--------------|--------|
| 135 | Shell-Exec Hardening | #51 | 1 | ✅ passed | PR #58 |
| 136 | Hub Queue Tenant-Binding | #51 | 2 | ✅ passed | PR #59 |
| 137 | DB Isolation & Pool | #49 | 1 | ✅ passed | PR #60 |
| 138 | Transactional Scan Unit-of-Work | #49 | 2 | ✅ passed | PR #61 |
| 139 | History Model, Run ID & Child Reconciliation | #49 | 2 | ✅ passed | PR #62 |
| 140 | Canonical Contract | #50 | 3 | ✅ passed | PR #63 |
| 141 | ScanService Unification | #50 | 3 | ✅ passed | PR #64 |
| 142 | Integration Correctness | #52 | 2 | ✅ passed | PR #68 |
| 143 | Scaling & Runtime Hardening | #52 | 5 | ✅ passed | PR #70 |

Plus **PR #69** — a mid-milestone CI hotfix (logger ENOENT on fresh install; also a real production bug).

## Requirements Coverage (38/38)

| Family | Phase(s) | Count | Status |
|--------|----------|-------|--------|
| SEC-01..08 | 135, 136 | 8 | ✅ satisfied |
| ISO-01..11 | 137, 138, 139 | 11 | ✅ satisfied |
| PIPE-01..04 | 141 | 4 | ✅ satisfied |
| CTR-01..05 | 140, 141 | 5 | ✅ satisfied |
| INTG-01..04 | 142 | 4 | ✅ satisfied |
| PERF-01..06 | 143 | 6 | ✅ satisfied |

All 38 checked `[x]` in REQUIREMENTS.md; each verified against shipped code by its phase VERIFICATION.md AND given a cross-phase integration touchpoint by the integration checker.

## Cross-Phase Integration (from gsd-integration-checker)

| Wiring chain | Status | Evidence |
|--------------|--------|----------|
| Scan write path 137→138→139→141→142→143 | **WIRED** | `manager.js`: bounded loop (`allSettledBounded`:826) → `persistScanResult`:888 → 138 `db.transaction()` (scan-service.js:75, beginScan inside) → 139 snapshot once post-loop (:1213, guarded :1208) → 142 Chroma per-repo (:1095). `r.scanVersionId` grep = **0** (138 rethread intact). No phase undid a prior. |
| persistScanResult = single write path + 140 contract | **WIRED** | all 4 transports call it (map.md:315, rescan.md:259, manager.js:888, http.js:744); http.js validates via `contract.js` (:727) before persist; scan-service.js is a pure module (only imports overrides.js). |
| Migrations 021–024 contiguous + consumed | **WIRED** | 021 kind/repos_json → drift-local.js; 022 source/target_symbol → persistFindings; 023 services.source_file → scan-service deleted-file cleanup; 024 perf indexes. Unique versions; benign pre-existing 012 gap. |
| DB handle lifecycle 137→143 | **WIRED** (⚠) | pool keyed by resolved dbPath; `closeAll` wired to worker shutdown (index.js:97); `sweepIdleEntries` unref'd. **Warning:** mid-scan eviction if a scan >10 min. |
| Security boundary 135/136 | **WIRED** | execFileSync argv (no shell) intact; queue rows bound to hub_url/org_id; no later phase touched hub-sync/ or reintroduced shell exec. |

**E2E flows:** `/arcanon:map` multi-repo, `/arcanon:rescan`, `/arcanon:drift graph`, MCP `impact_scan`→HTTP `/scan` — **all 4 OK**.

**Test suite:** 1124/1124 pass, 0 fail, no snapshots leak.

## Recommended follow-ups (all non-blocking — none gate the milestone)

1. **PERF-05 mid-scan eviction** (real latent bug, low probability): re-`_touch` the pool handle at Phase B write time, or integrate the scan-lock with `sweepIdleEntries`, so a >10-min scan can't have its live handle closed mid-flight.
2. **CTR-04 defense-in-depth**: route agent findings through `validateFindings` in the manager path too (currently only pre-parsed via findings.js shim).
3. **Process**: mark the four CI checks *required* in `main` branch protection (repo-admin), so a red PR can't auto-merge again.
4. Optional cleanup: the pre-existing `TODO(post-v0.1.3)` prose_evidence_warnings counter.

None block completion. Recommend proceeding to `/gsd-complete-milestone v0.2.0`.
